import { AxiosInstance } from 'axios';
import { Asset, Status } from '../common';
export interface GetInvoices {
    asset: Asset;
    invoice_ids?: Array<string | number>;
    status?: Status;
    offset?: number;
    count?: number;
}
export declare const getInvoices: (instance: AxiosInstance, token: string) => (values: GetInvoices) => Promise<any>;
