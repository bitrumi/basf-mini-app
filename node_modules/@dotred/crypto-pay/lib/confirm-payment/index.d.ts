import { AxiosInstance } from 'axios';
export interface ConfirmPayment {
    invoice_id: number;
}
export declare const confirmPayment: (instance: AxiosInstance, token: string) => (invoice_id: number) => Promise<any>;
