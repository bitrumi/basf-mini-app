import { AxiosInstance } from 'axios';
import { Asset } from '../common';
export interface CreateInvoice {
    asset: Asset;
    amount: string | number;
    description?: string;
    paid_btn_name?: "viewItem" | "openChannel" | "openBot" | "callback";
    paid_btn_url?: string;
    payload?: string;
}
export declare const createInvoice: (instance: AxiosInstance, token: string) => (values: CreateInvoice) => Promise<any>;
