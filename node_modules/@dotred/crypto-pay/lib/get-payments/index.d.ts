import { AxiosInstance } from 'axios';
export interface GetPayments {
    offset?: number;
    count?: number;
}
export declare const getPayments: (instance: AxiosInstance, token: string) => (values: GetPayments) => Promise<any>;
